<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Matricis.ViewModels"
             x:Class="Matricis.Views.CriteriaDetailPage">
    <ContentPage.BindingContext>
        <vm:CriteriaDetailViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Criteria.Descripion}" />
            <Label Text="{Binding Criteria.Title}" />
            <Label Text="{Binding Criteria.Id}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>